eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka
management:
  endpoint:
    health:
      show-details: always
welcome.message: 'Hello! I am the transformer'