eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka
spring:
  application:
    name: products
  h2:
    console:
      enabled: 'true'
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server